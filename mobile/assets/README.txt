Placeholder folder.

This project intentionally ships without binary assets.
The Room Manager logo and all icons are rendered as inline SVG
via react-native-svg (see src/components/Logo.tsx and
src/components/Icon.tsx). Splash and app icon use Expo defaults
backed by the dark page color (#171717) configured in app.json.
